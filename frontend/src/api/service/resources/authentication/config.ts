const domain = 'auth/';

const config = {
    path: {
        register: domain + 'register',
        login: domain + 'login',
        logout: domain + 'logout',
        refresh: domain + 'refresh',
    },
};

export default config;
