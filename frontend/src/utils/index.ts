import jwt from './jwt';
import regex from './regex';
import time from './time';
import validators from './validators';

const utils = {
    time,
    jwt,
    validators,
    regex,
};

export default utils;
